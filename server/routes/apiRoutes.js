import express from 'express';

export const userRoutes {


  const router = express.Router();

  router.get('/', (res, req) => {
    return res.json({
      centers,
      errors: false
    })
  })
};